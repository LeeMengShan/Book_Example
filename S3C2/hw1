/*

Author: Meng-Shan Lee

ToDo:
拿一隻紙筆重新抄寫Flutter專案資料夾內各資料夾的應用方向

*/


android
	由於flutter支援多載具，包括Android，為了能夠順利支援Android，這個android資料夾內放的內容多與Android系統相關。

build
	程式碼寫完之後，需要編譯成執行檔，執行檔，預設會放在build資料夾

ios
	由於flutter支援多載具，包括IOS，為了能夠順利支援IOS，這個ios資料夾內放的內容多與IOS系統相關。

lib
	專案程式碼資料夾，裡面還有多種資料夾的細分。

test
	專門存放那些測試專案程式碼是否能正常執行的測試用程式碼。

web
  由於flutter支援多載具，包括Web，為了能夠順利支援Web，這個web資料夾內放的內容多與web系統相關。

pubspec.yml
	在開發專案程式碼的時候，會需要一些額外功能，為了這些功能，我們會額外去下載一些套件，為了方便管理套件的下載安裝，會將套件名與版本號寫在此。

README.md
	開發者會在此介紹專案的功能、使用方式、程式碼結構等

.gitignore
	有的時候，一個專案會由多位工程師開發完成，會需要分享檔案，為了避免機敏性檔案外洩，我們可以將機敏性檔案的檔名寫在此，以後使用git程式做共享時，就會自動趨避了。
